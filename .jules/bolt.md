## 2024-06-25 - Cache Stampedes in Node.js\n**Learning:** Implementing naive caching with raw values in an async system (like Express routing hitting external APIs) does not protect against concurrent requests stampeding the system when the cache is cold or expired.\n**Action:** Use Promise caching (storing `Promise<Value>`) instead of raw value caching when coalescing concurrent asynchronous requests is needed.
